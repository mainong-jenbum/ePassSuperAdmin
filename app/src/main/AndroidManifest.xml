<manifest xmlns:android = "http://schemas.android.com/apk/res/android"
    package = "com.jenbumapps.epass_superadmin">

    <uses-permission android:name = "android.permission.INTERNET" />

    <application
        android:allowBackup = "true"
        android:icon = "@drawable/ic_logo"
        android:label = "@string/app_name"
        android:networkSecurityConfig = "@xml/network_security_config"
        android:roundIcon = "@drawable/ic_logo"
        android:supportsRtl = "true"
        android:theme = "@style/AppTheme">
        <activity android:name = ".ui.MainActivity">
            <intent-filter>
                <action android:name = "android.intent.action.MAIN" />

                <category android:name = "android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name = ".ui.permit.CityPermitActivity" />
        <activity android:name = ".ui.permit.PermitListActivity" />
        <activity android:name = ".ui.division.DivisionActivity" />
        <activity android:name = ".ui.user.UserListActivity" />
        <activity android:name = ".ui.user.UserAddActivity" />
    </application>

</manifest>